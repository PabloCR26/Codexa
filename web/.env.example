# En desarrollo se deja vacío: el proxy de Vite reenvía /api al backend,
# así el navegador ve un solo origen (sin problemas de CORS ni de cookies).
VITE_API_URL=

# En Docker o producción apunta al origen público de la API, por ejemplo:
# VITE_API_URL=http://localhost:4000
