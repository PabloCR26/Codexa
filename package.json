{
  "name": "flowhub-api",
  "version": "0.1.0",
  "private": true,
  "description": "API productora de FlowHub",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/api/index.js",
    "start": "node src/api/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bullmq": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "helmet": "^8.0.0",
    "ioredis": "^5.4.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
